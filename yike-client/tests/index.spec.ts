import { expect, test } from 'vitest'
import LoadingBox from '../src/components/LoadingBox'

test('first test', () => {
  expect(1 + 1).toBe(2)
})

test('hello', () => {
  console.log(LoadingBox.toString())
})
