import React from 'react'
import MediaBox from './MediaBox'

describe('<MediaBox />', () => {
  it('renders', () => {
    // see: https://on.cypress.io/mounting-react
    cy.mount(<MediaBox srcObject={} />)
  })
})
